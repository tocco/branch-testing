{
  "name": "branch-testing",
  "version": "1.0.0",
  "description": "test repo to play around with release branches and auto-merging",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "changelog": "./changelog.sh"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^3.0.0-beta.20"
  }
}
